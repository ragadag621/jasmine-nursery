import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function NotFoundPage() {
  return <PagePlaceholder titleHe="404 — הדף לא נמצא" note="בדקו את הכתובת ונסו שוב." />;
}
